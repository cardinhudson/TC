# ==========================================
# IMPORTANTE: VERSÕES ESPECÍFICAS OBRIGATÓRIAS
# ==========================================
# Este arquivo usa versões EXATAS (==) em vez de >= para garantir
# que todos os PCs tenham exatamente as mesmas versões instaladas.
#
# PROBLEMA COMUM: Se usar >=, diferentes PCs podem instalar versões
# diferentes das bibliotecas, causando problemas de renderização de
# tabelas, gráficos e comportamento inconsistente do Streamlit.
#
# SOLUÇÃO: Sempre instale as versões EXATAS especificadas abaixo.
# Se houver problemas, desinstale tudo e reinstale usando este arquivo:
#   pip uninstall streamlit pandas numpy altair plotly openpyxl -y
#   pip install -r requirements.txt
# ==========================================

streamlit==1.50.0
# Versão específica necessária para compatibilidade com st.expander
# Versões anteriores podem causar erros ao usar expanders
# NOTA: O código foi ajustado para evitar expanders aninhados em 3 níveis
# (substituídos por containers no nível 3) para evitar problemas de renderização

pandas==2.3.3
# Versão específica para garantir renderização consistente de tabelas
# Versões diferentes podem causar problemas no st.dataframe() e na
# formatação de DataFrames, especialmente com tipos category e downcast

numpy==2.3.5
# Versão específica compatível com pandas 2.3.3
# Mudanças no numpy podem afetar processamento de dados numéricos

altair==5.5.0
# Versão específica para gráficos Altair
# Compatível com Streamlit 1.50.0

plotly==6.5.0
# Versão específica para gráficos Plotly
# Versões diferentes podem causar problemas de renderização

openpyxl==3.1.5
# Versão específica para leitura/escrita de arquivos Excel
# Necessário para processar arquivos .xlsx

pyarrow==18.1.0
# Versão específica para leitura/escrita de arquivos Parquet
# Necessário para processar arquivos .parquet (formato usado para dados históricos e forecast)

streamlit-aggrid==1.2.1
# Versão específica para tabelas interativas AgGrid no Streamlit
# Usado na funcionalidade de custos específicos para exibição e seleção de linhas

# ==========================================
# TC COPILOT — Relatório Anual com IA
# ==========================================
APScheduler==3.10.4
# Agendamento de tarefas (envio diário de alertas)

msal>=1.28.0
# Microsoft Authentication Library — OAuth 2.0 para Graph API (envio de e-mail)

openai>=1.0.0
# SDK oficial da OpenAI para integração com GPT

reportlab>=4.0
# Geração de PDFs profissionais com ReportLab

matplotlib>=3.8.0
# Gráficos waterfall para inclusão no relatório PDF (TC Copilot)

python-dotenv>=1.0.0
# Carregamento de variáveis de ambiente do arquivo .env

certifi>=2024.0.0
# CA bundle explícito para webhooks HTTPS no executável

truststore>=0.10.0
# Usa o repositório nativo de certificados do Windows no EXE

PyPDF2>=3.0.0
# Extração de texto de PDFs para consulta inteligente

python-pptx>=0.6.23
# Geração de apresentações PowerPoint nativas para exportação da apresentação visual

# ==========================================
# NOTA: DEPENDÊNCIAS DO CHATBOT SÃO OPCIONAIS
# ==========================================
# As dependências do chatbot (sentence-transformers, faiss-cpu, torch, transformers)
# foram movidas para requirements-chatbot.txt para evitar problemas em deploys.
# O chatbot funciona sem elas usando método de busca simples (fallback).
# 
# Para instalar as dependências do chatbot (melhor qualidade de respostas):
#   pip install -r requirements-chatbot.txt
#
# OU instalar tudo de uma vez:
#   pip install -r requirements.txt -r requirements-chatbot.txt
